
const fetcher = require('../dist/index');

(async function(){
    let res = await fetcher.fetch({
        clientId: '',
        clientSecret: '',
        refreshToken: '',
    });
    console.log(res);
})()
